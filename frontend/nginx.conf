server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # WAŻNE: Poniżej są spacje, nie przecinki!
        try_files $uri $uri/ /index.html;
    }
}